/**
 * A placeholder UI component that renders a card with rounded
 * corners and a shadow.
 * @param {*} props accepts the elements enclosed inside the
 * JSX Tag as the children prop.
 */
const Card = (props) => {
  <div className="card">{props.children}</div>;
};

export default Card;
